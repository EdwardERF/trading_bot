funcion buscarPivotesTop

do while (contador < 2):

    for i in range(len(candles)):
        if(candles[i]) es mayor a 4 velas menos y 4 velas mas:
            if (contador != 1):
                pivoteFinal = candles[i]
                contador++
            elif (contador = 1):
                pivoteInicial = candles[i]
        else
            loop
        
        return pivoteInicial, pivoteFinal

// otra funcion: crear recta con esos dos puntos
